using UnityEngine;

namespace LanternDrift.Gameplay
{
    // Legacy component kept only for compatibility with older generated scenes.
    // Sandbars now act as raised solid walls rather than trigger-based slow zones.
    public class SandbarZone : MonoBehaviour
    {
    }
}
